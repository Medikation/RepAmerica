{{ 'rep-america-about.css' | asset_url | stylesheet_tag }}
<section class="ra-about ra-about-cta">
  <div class="ra-about__shell">
    <div class="ra-about-cta__inner">
      <p class="ra-about__eyebrow">{{ section.settings.eyebrow | escape }}</p>
      <h2 class="ra-about__section-heading">{{ section.settings.heading | escape }}</h2>
      <div class="ra-about__body ra-about-cta__body">{{ section.settings.body }}</div>

      {%- if section.settings.button_label != blank and section.settings.button_link != blank -%}
        <a class="ra-about-cta__button" href="{{ section.settings.button_link }}">
          {{ section.settings.button_label | escape }}
        </a>
      {%- endif -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Workshop call to action",
  "tag": "section",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "The Great Books Project" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Explore the Book List." },
    {
      "type": "richtext",
      "id": "body",
      "label": "Body",
      "default": "<p>Explore the books, ideas, and thinkers that shaped Western civilization. Every title links directly to the edition or translation I recommend.</p>"
    },
    { "type": "text", "id": "button_label", "label": "Button label", "default": "Explore the Book List" },
    { "type": "url", "id": "button_link", "label": "Button link" }
  ],
  "presets": [
    { "name": "Workshop call to action" }
  ]
}
{% endschema %}
